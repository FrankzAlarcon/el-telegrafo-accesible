'use client'

import { type FC } from 'react'
import ReactSwitch from 'react-switch'

const SwitchComponent: FC<any> = () => {
  return (
    <ReactSwitch checked onChange={() => {}} />
  )
}

export default SwitchComponent

export interface WithSlugParam {
  params: {
    slug: string
  }
}

export interface WithLevelParam {
  params: {
    level: string
  }
}

export interface WithSearchParam {
  params: {
    search: string
  }
}
